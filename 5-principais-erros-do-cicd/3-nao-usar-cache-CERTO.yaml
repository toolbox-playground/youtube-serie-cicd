# ✅ COM CACHE: usa pacotes já baixados
steps:
  - uses: actions/checkout@v4

  - uses: actions/setup-node@v4
    with:
      node-version: '20'
      cache: 'npm'   # cache automático pelo hash do package-lock

  - run: npm ci      # usa o lock file para instalar
  - run: npm test
